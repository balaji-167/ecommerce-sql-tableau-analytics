# SQL Analysis Summary

## Overview

This project uses PostgreSQL to transform raw e-commerce data into clean, structured, and business-ready analytical outputs for Tableau dashboarding.

The SQL workflow follows a Medallion Architecture:

```text
Bronze Layer → Silver Layer → Gold Layer → Tableau Dashboards
